add_nextcode_target_library(nextcodeCommandLineSupport
  STATIC DONT_EMBED_BITCODE NONEXTCODERT
  CommandLine.cpp
  C_COMPILE_FLAGS ${NEXTCODE_RUNTIME_CXX_FLAGS}
  LINK_FLAGS ${NEXTCODE_RUNTIME_LINK_FLAGS}
  NEXTCODE_COMPILE_FLAGS ${NEXTCODE_STANDARD_LIBRARY_NEXTCODE_FLAGS}
  INSTALL_IN_COMPONENT never_install
  )
