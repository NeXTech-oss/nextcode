add_nextcode_target_library(nextcodeWinSDK ${NEXTCODE_STDLIB_LIBRARY_BUILD_TYPES} IS_SDK_OVERLAY
    WinSDK.code

    NEXTCODE_COMPILE_FLAGS ${NEXTCODE_RUNTIME_NEXTCODE_COMPILE_FLAGS} ${NEXTCODE_STANDARD_LIBRARY_NEXTCODE_FLAGS}
    LINK_FLAGS "${NEXTCODE_RUNTIME_NEXTCODE_LINK_FLAGS}"
    TARGET_SDKS WINDOWS
    INSTALL_IN_COMPONENT sdk-overlay)

